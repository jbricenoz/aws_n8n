# Dockerfile for running N8N
FROM n8nio/n8n:latest

# You can add custom configuration or plugins here if needed
# For example, to install additional packages:
# RUN npm install <package>

# Expose the default N8N port
EXPOSE 5678
